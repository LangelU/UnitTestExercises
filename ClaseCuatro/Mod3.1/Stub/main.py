# 1. Ejercicio Fácil: Crea un stub para simular la respuesta de un servicio de
# autenticación. La función debe devolver un valor predefinido (por ejemplo,
# "token12345") cuando se le pasa un nombre de usuario y contraseña válidos.

class ApiService:
    def authenticate(self):
        """
        Autenticarse en una api externa.

        Args:

        Returns:
        """
        # Lógica de autenticación
        pass
